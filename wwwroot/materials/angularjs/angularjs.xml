<lesson>
    <chapter>
        <level>
            Beginner
        </level>
        <title>
            Introduction
        </title>
        <content>
            What is AngularJS?

            AngularJS is a structural framework for dynamic web apps.It lets you use HTML as your template language and lets you extend HTML's syntax to express your application's components clearly and succinctly. Angular's data binding and dependency injection eliminate much of the code you would otherwise have to write. And it all happens within the browser, making it an ideal partner with any server technology.

            Angular is what HTML would have been, had it been designed for applications.

            Try running the sample code on our Code Editor.

            Code Explanation:

            The controller creates two properties (variables) in the scope (firstName and lastName).

            The ng-bind directive binds the innerHTML of the h1 element to the application variable firstName and lastName.
        </content>
        <code>
            <htmlcode>
                <![CDATA[
                    <div ng-app="myApp" ng-controller="myController">
                    /t <h1 ng-bind="firstName"></h1>
                    /t <h1 ng-bind="lastName"></h1>
                    </div>
                ]]>
            </htmlcode>
            <scriptcode>
                <![CDATA[
                    var app = angular.module('myApp', []);

                    app.controller('myController', ['$scope', function ($scope) {
                    /t $scope.firstName = 'John';
                    /t $scope.lastName = 'Doe';
                    }]);
                 ]]>
            </scriptcode>
            <stylecode>
                <![CDATA[

                 ]]>
            </stylecode>
        </code>
        <example>
            <htmlcode>
                <![CDATA[
                    <div ng-app="myApp" ng-controller="myController">
                    /t <h1 ng-bind="firstName"></h1>
                    /t <h1 ng-bind="lastName"></h1>
                    </div>
                ]]>
            </htmlcode>
            <scriptcode>
                <![CDATA[
                    var app = angular.module('myApp', []);

                    app.controller('myController', ['$scope', function ($scope) {
                    /t $scope.firstName = 'John';
                    /t $scope.lastName = 'Doe';
                    }]);
                 ]]>
            </scriptcode>
            <stylecode>
                <![CDATA[

                 ]]>
            </stylecode>
        </example>
    </chapter>
    <chapter>
        <level>
            Beginner
        </level>
        <title>
            Directives
        </title>
        <content>
            At a high level, directives are markers on a DOM element (such as an attribute, element name, comment or CSS class) that tell AngularJS's HTML compiler ($compile) to attach a specified behavior to that DOM element (e.g. via event listeners), or even to transform the DOM element and its children.

            Angular comes with a set of these directives built-in, like ngBind, ngModel, and ngClass.

            AngularJS directives are extended HTML attributes with the prefix ng-.

            The ng-app directive initializes an AngularJS application.

            The ng-init directive initializes application data.

            The ng-model directive binds the value of HTML controls (input, select, textarea) to application data.

            Example:
            
                &lt;div ng-app="" ng-init="firstName='John'">

                    &lt;p>Name: &lt;input type="text" ng-model="firstName">&lt;/p>
                    &lt;p>You wrote: {{ firstName }}&lt;/p>

                &lt;/div>

            Repeating HTML Elements

            The ng-repeat directive repeats an HTML element.

            Example:

            &lt;div ng-app="" ng-init="names=['Jani','Hege','Kai']">
                &lt;ul>
                    &lt;li ng-repeat="x in names">
                        {{ x }}
                    &lt;/li>
                &lt;/ul>
            &lt;/div>

            You can create your own custom directive using the angular directive() function.

            Exercise:

            1. Try to supply the missing Directives on the example on our Code Editor. When you're done you'll have a text input and anything that you type on it will be shown below it.
        
            2. Try to output at least five names using ng-repeat directive. 
        </content>
        <code>
            <htmlcode>
                <![CDATA[
                    <div ng-app="">
                    /t Exercise 1
                    /t<div>
                    /t/t <p>Name: <input type="text"></p>
                    /t/t <p>You wrote: {{ firstName }}</p>
                    /t</div>
                    
                    /t Exercise 2
                    /t <div ng-init="">
                    /t/t <ul>
                    /t/t/t <li></li>
                    /t/t </ul>
                    /t </div>
                    </div>
                ]]>
            </htmlcode>
            <scriptcode>
                <![CDATA[
                    
                 ]]>
            </scriptcode>
            <stylecode>
                <![CDATA[

                 ]]>
            </stylecode>
        </code>
        <example>
            <htmlcode>
                <![CDATA[
                    <div ng-app="">
                    /t Exercise 1
                    /t<div ng-init="firstName = 'John'">
                    /t/t <p>Name: <input type="text" ng-model="firstName"></p>
                    /t/t <p>You wrote: {{ firstName }}</p>
                    /t</div>
                    
                    /t Exercise 2
                    /t <div ng-init="names=['Peter','Sebastian','Erika','Patrick','Samantha']">
                    /t/t <ul>
                    /t/t/t <li ng-repeat="x in names">{{x}}</li>
                    /t/t </ul>
                    /t </div>
                    </div>
                ]]>
            </htmlcode>
            <scriptcode>
                <![CDATA[
                    
                 ]]>
            </scriptcode>
            <stylecode>
                <![CDATA[

                 ]]>
            </stylecode>
        </example>
    </chapter>
    <chapter>
        <level>
            Beginner
        </level>
        <title>
            Expressions
        </title>
        <content>
            Angular expressions are JavaScript-like code snippets that are mainly placed in interpolation bindings.

            AngularJS expressions can be written inside double braces: {{ expression }}.

            AngularJS expressions can also be written inside a directive: ng-bind="expression".

            AngularJS will resolve the expression, and return the result exactly where the expression is written.

            AngularJS expressions are much like JavaScript expressions: They can contain literals, operators, and variables.

            Example:

                &lt;div ng-app="">

                    Numbers
                    &lt;div ng-init="quantity=1;cost=5">

                        &lt;p>Total in dollar: &lt;span ng-bind="quantity * cost">&lt;/span>&lt;/p>

                    &lt;/div>

                    Strings
                    &lt;div ng-init="firstName='John';lastName='Doe'">

                        &lt;p>The name is {{ firstName + " " + lastName }}&lt;/p>

                    &lt;/div>

                    Objects
                    &lt;div ng-init="person={firstName:'John',lastName:'Doe'}">

                        &lt;p>The name is {{ person.lastName }}&lt;/p>

                    &lt;/div>

                    Arrays
                    &lt;div ng-init="points=[1,15,19,2,40]">

                        &lt;p>The third result is {{ points[2] }}&lt;/p>

                    &lt;/div>

                &lt;/div>

            One-time binding

            An expression that starts with :: is considered a one-time expression. One-time expressions will stop recalculating once they are stable, which happens after the first digest if the expression result is a non-undefined value.

            Reasons for using one-time binding
            The main purpose of one-time binding expression is to provide a way to create a binding that gets deregistered and frees up resources once the binding is stabilized. Reducing the number of expressions being watched makes the digest loop faster and allows more information to be displayed at the same time.

            Exercise:

            Refer to our example and try to recreate them in our Code Editor.
        </content>
        <code>
            <htmlcode>
                <![CDATA[
                    <div ng-app="">
                    /t <h4>Numbers</h4>
                    /t <div ng-init="">
                    /t/t <p></p>
                    /t </div>

                    /t <h4>Strings</h4>
                    /t <div ng-init="">
                    /t/t <p></p>
                    /t </div>

                    /t <h4>Objects</h4>
                    /t <div ng-init="">
                    /t/t <p></p>
                    /t </div>

                    /t <h4>Arrays</h4>
                    /t <div ng-init="">
                    /t/t <p></p>
                    /t </div>
                    </div>
                ]]>
            </htmlcode>
            <scriptcode>
                <![CDATA[
                    
                 ]]>
            </scriptcode>
            <stylecode>
                <![CDATA[

                 ]]>
            </stylecode>
        </code>
        <example>
            <htmlcode>
                <![CDATA[
                    <div ng-app="">
                    /t <h4>Numbers</h4>
                    /t <div ng-init="quantity=1;cost=5"> 
                    /t/t <p>Total: <span ng-bind="quantity * cost"></span></p>
                    /t </div> 

                    /t <h4>Strings</h4>
                    /t <div ng-init="firstName='John';lastName='Doe'">
                    /t/t <p>The name is {{ firstName + " " + lastName }}</p>
                    /t </div>

                    /t <h4>Objects</h4>
                    /t <div ng-init="person={firstName:'John',lastName:'Doe'}">
                    /t/t <p>The name is {{ person.firstName + " " + person.lastName }}</p>
                    /t </div>
                      
                    /t <h4>Arrays</h4>
                    /t <div ng-init="points=[1,15,19,2,40]">
                    /t/t <p>The third result is {{ points[2] }}</p>
                    /t </div>
                    </div>
                ]]>
            </htmlcode>
            <scriptcode>
                <![CDATA[
                    
                 ]]>
            </scriptcode>
            <stylecode>
                <![CDATA[

                 ]]>
            </stylecode>
        </example>
    </chapter>
    <chapter>
        <level>
            Beginner
        </level>
        <title>
            Modules
        </title>
        <content>
            Modules define AngularJS applications. You can think of a module as a container for the different
            parts of your app – controllers, services, filters, directives, etc.

            A module is created by using the AngularJS function angular.module

            Example:

                var app = angular.module("myApp", []);            

            Exercise:

            Try to create the module of our app in the Code Editor to make it work.
        </content>
        <code>
            <htmlcode>
                <![CDATA[
                    <div ng-app="myApp" ng-controller="myController">
                    /t <h1>{{firstName}} {{lastName}}</h1>
                    </div>
                ]]>
            </htmlcode>
            <scriptcode>
                <![CDATA[
                    // Write your module here!

                    app.controller('myController', ['$scope', function ($scope) {
                    /t $scope.firstName = 'John';
                    /t $scope.lastName = 'Doe';
                    }]);
                 ]]>
            </scriptcode>
            <stylecode>
                <![CDATA[

                 ]]>
            </stylecode>
        </code>
        <example>
            <htmlcode>
                <![CDATA[
                    <div ng-app="myApp" ng-controller="myController">
                    /t <h1>{{firstName}} {{lastName}}</h1>
                    </div>
                ]]>
            </htmlcode>
            <scriptcode>
                <![CDATA[
                    var app = angular.module('myApp', []);

                    app.controller('myController', ['$scope', function ($scope) {
                    /t $scope.firstName = 'John';
                    /t $scope.lastName = 'Doe';
                    }]);
                 ]]>
            </scriptcode>
            <stylecode>
                <![CDATA[

                 ]]>
            </stylecode>
        </example>
    </chapter>
    <chapter>
        <level>
            Beginner
        </level>
        <title>
            Controllers
        </title>
        <content>
            In Angular, a Controller is defined by a JavaScript constructor function that is used to augment the Angular Scope.

            When a Controller is attached to the DOM via the ng-controller directive, Angular will instantiate a new Controller object, using the specified Controller's constructor function. A new child scope will be created and made available as an injectable parameter to the Controller's constructor function as $scope.

            Use controllers to:

            Set up the initial state of the $scope object.
            Add behavior to the $scope object.

            Do not use controllers to:

            Manipulate DOM — Controllers should contain only business logic. Putting any presentation logic into Controllers significantly
            affects its testability. Angular has databinding for most cases and directives to encapsulate manual DOM manipulation.
            Format input — Use angular form controls instead.
            Filter output — Use angular filters instead.
            Share code or state across controllers — Use angular services instead.
            Manage the life-cycle of other components (for example, to create service instances).

            The ng-controller directive defines the application controller.

            Example:

            &lt;div ng-app="myApp" ng-controller="myController">

                First Name: &lt;input type="text" ng-model="firstName">&lt;br>
                Last Name: &lt;input type="text" ng-model="lastName">&lt;br>
                &lt;&lt;br>
                Full Name: {{firstName + " " + lastName}}

            &lt;/div>

            &lt;script>
                var app = angular.module('myApp', []);
                app.controller('myController', function($scope) {
                    $scope.firstName = "John";
                    $scope.lastName = "Doe";
                });
            &lt;/script>

            Exercise:

            Create your own controller and supply a value for each scope so that the expressions in the View will output a value.
        </content>
        <code>
            <htmlcode>
                <![CDATA[
                    <div ng-app="myApp" ng-controller="">
                    /t <h1>{{scopeOne}}</h1>
                    /t <h1>{{scopeTwo}}</h1>
                    /t <h1>{{scopeThree}}</h1>
                    </div>
                ]]>
            </htmlcode>
            <scriptcode>
                <![CDATA[
                    var app = angular.module('myApp', []);
                    
                    // Write your Controller here!
                 ]]>
            </scriptcode>
            <stylecode>
                <![CDATA[

                 ]]>
            </stylecode>
        </code>
        <example>
            <htmlcode>
                <![CDATA[
                    <div ng-app="myApp" ng-controller="myController">
                    /t <h1>scopeOne: {{scopeOne}}</h1>
                    /t <h1>scopeTwo: {{scopeTwo}}</h1>
                    /t <h1>scopeThree: {{scopeThree.name + " " + scopeThree.age}}</h1>
                    </div>
                ]]>
            </htmlcode>
            <scriptcode>
                <![CDATA[
                    var app = angular.module('myApp', []);
                    
                    app.controller('myController', ['$scope', function($scope) {
                    /t $scope.scopeOne = 'John Doe';
                    /t $scope.scopeTwo = 42;
                    /t $scope.scopeThree = {'name':'John','age':42};
                    }]);
                 ]]>
            </scriptcode>
            <stylecode>
                <![CDATA[

                 ]]>
            </stylecode>
        </example>
    </chapter>
    <chapter>
        <level>
            Beginner
        </level>
        <title>
            Scopes
        </title>
        <content>
            Scope is an object that refers to the application model. It is an execution context for expressions.
            Scope is the binding part between the HTML (view) and the JavaScript (controller).
            Scope is available for both the view and the controller.

            When you make a controller in AngularJS, you pass the $scope object as an argument.

            Understanding the Scope
            If we consider an AngularJS application to consist of:

            View, which is the HTML.
            Model, which is the data available for the current view.
            Controller, which is the JavaScript function that makes, changes, removes, or controls the data.
            Then the scope is the Model.

            Example:

            &lt;div ng-app="myApp" ng-controller="myCtrl">

                &lt;h1>{{carname}}&lt;/h1>

            &lt;/div>

            &lt;script>
                var app = angular.module('myApp', []);

                app.controller('myCtrl', function($scope) {
                    $scope.carname = "Volvo";
                });
            &lt;/script>

            Root Scope
            All applications have a $rootScope which is the scope created on the HTML element that contains the ng-app directive.

            The rootScope is available in the entire application.

            Exercise:

            Create atleast three scopes in the controller and output them in the View using Expressions.
        </content>
        <code>
            <htmlcode>
                <![CDATA[
                    <div ng-app="myApp" ng-controller="myController">
                    <!-- Write your Expressions here! -->
                    </div>
                ]]>
            </htmlcode>
            <scriptcode>
                <![CDATA[
                    var app = angular.module('myApp', []);

                    app.controller('myController', ['$scope', function ($scope) {
                    // Write your scopes here!
                    }]);
                 ]]>
            </scriptcode>
            <stylecode>
                <![CDATA[

                 ]]>
            </stylecode>
        </code>
        <example>
            <htmlcode>
                <![CDATA[
                    <div ng-app="myApp" ng-controller="myController">
                    /t <h3>{{carOne}}</h3>
                    /t <h3>{{carTwo}}</h3>
                    /t <h3>{{carThree}}</h3>
                    </div>
                ]]>
            </htmlcode>
            <scriptcode>
                <![CDATA[
                    var app = angular.module('myApp', []);

                    app.controller('myController', ['$scope', function ($scope) {
                    /t $scope.carOne = 'Volvo';
                    /t $scope.carTwo = 'Audi';
                    /t $scope.carThree = 'Mercedes';
                    }]);
                 ]]>
            </scriptcode>
            <stylecode>
                <![CDATA[

                 ]]>
            </stylecode>
        </example>
    </chapter>
    <chapter>
        <level>
            Beginner
        </level>
        <title>
            Data Binding
        </title>
        <content>
            Data-binding in Angular apps is the automatic synchronization of data between the model and view components.
            The way that Angular implements data-binding lets you treat the model as the single-source-of-truth in your application.
            The view is a projection of the model at all times. When the model changes, the view reflects the change, and vice versa.

            Data Model
            AngularJS applications usually have a data model. The data model is a collection of data available for the application.
        
            Example:

            var app = angular.module('myApp', []);
            app.controller('myCtrl', function($scope) {
                $scope.firstname = "John";
                $scope.lastname = "Doe";
            });

            HTML View
            The HTML container where the AngularJS application is displayed, is called the view.

            The view has access to the model, and there are several ways of displaying model data in the view.

            You can use the ng-bind directive, which will bind the innerHTML of the element to the specified model property:

            Example:

            &lt;p ng-bind="firstname">&lt;/p>

            You can also use double braces {{ }} to display content from the model:

            &lt;p>First name: {{firstname}}&lt;/p>

            Or you can use the ng-model directive on HTML controls to bind the model to the view.

            &lt;input ng-model="firstname">

            The ng-model directive provides a two-way binding between the model and the view.

            Two-way Binding
            Data binding in AngularJS is the synchronization between the model and the view.

            When data in the model changes, the view reflects the change, and when data in the view changes, the model is updated as well.
            This happens immediately and automatically, which makes sure that the model and the view is updated at all times.

            Example:

            &lt;div ng-app="myApp" ng-controller="myCtrl">
                Name: &lt;input ng-model="firstname">
                &lt;h1>{{firstname}}&lt;/h1>
            &lt;/div>

            &lt;script>
                var app = angular.module('myApp', []);
                app.controller('myCtrl', function($scope) {
                    $scope.firstname = "John";
                    $scope.lastname = "Doe";
                });
            &lt;/script>

            Exercise:

            Refer on our examples then create your own Two-way Binding.
        </content>
        <code>
            <htmlcode>
                <![CDATA[
                    <div ng-app="myApp" ng-controller="myCtrl">
                    
                    </div>
                ]]>
            </htmlcode>
            <scriptcode>
                <![CDATA[
                    var app = angular.module('myApp', []);

                    app.controller('myCtrl', function($scope) {
                    
                    });
                 ]]>
            </scriptcode>
            <stylecode>
                <![CDATA[

                 ]]>
            </stylecode>
        </code>
        <example>
            <htmlcode>
                <![CDATA[
                    <div ng-app="myApp" ng-controller="myCtrl">
                    /t Name: <input ng-model="firstname">
                    /t <h1>{{firstname}}</h1>
                    </div>
                ]]>
            </htmlcode>
            <scriptcode>
                <![CDATA[
                    var app = angular.module('myApp', []);

                    app.controller('myCtrl', function($scope) {
                    /t $scope.firstname = 'John';
                    /t $scope.lastname = 'Doe';
                    });
                 ]]>
            </scriptcode>
            <stylecode>
                <![CDATA[

                 ]]>
            </stylecode>
        </example>
    </chapter>
    <chapter>
        <level>
            Intermediate
        </level>
        <title>
            Services
        </title>
        <content>
            Angular services are substitutable objects that are wired together using dependency injection (DI).
            You can use services to organize and share code across your app.

            Angular services are:

            Lazily instantiated – Angular only instantiates a service when an application component depends on it.
            Singletons – Each component dependent on a service gets a reference to the single instance generated by the service factory.
            Angular offers several useful services (like $http), but for most applications you'll also want to create your own.

            To use an Angular service, you add it as a dependency for the component (controller, service, filter or directive) that depends on the service.
            Angular's dependency injection subsystem takes care of the rest.
        
            Example:

            app.controller('myCtrl', function($scope, MyService, PromiseService) {
                $scope.myCustomService = MyService.myFunc();

                PromiseService.getDetails().then(function (response) {
                    $scope.details = response.data;
                });
            });

            You can create your own custom service using the angular service() function.

            Exercise:

            1. On our code editor we have a service that convert your input to Uppercase. Make another service that convert your input to Lowercase. Don't forget to add it on your Controller.

            2. Notice we have another service in our example called AnotherService. It returns a JSON object as a promise so you will need to use then() function. Use this service and bind its response to the scope then use ng-repeat in our View to show only the names.
        </content>
        <code>
            <htmlcode>
                <![CDATA[
                    <div ng-app="myApp" ng-controller="myController">
                    /t <h4>Service Example</h4>
                    /t <input ng-model="myInput" ng-change="toUpperCaseFunction(myInput)">
                    /t <h2>{{myOutput}}</h2>

                    /t <h4>Exercise 1</h4>
                    /t <!-- Write here! -->
                    /t <h4>Exercise 2</h4>
                    /t <!-- Write here! -->
                    </div>
                ]]>
            </htmlcode>
            <scriptcode>
                <![CDATA[
                    var app = angular.module('myApp', []);

                    app.service('MyService', function() {
                    /t this.myFunction = function (data) {
                    /t/t return data.toUpperCase();
                    /t }
                    });

                    app.service('AnotherService', ['$http', function($http) {
                    /t this.getDetails = function () {
                    /t/t return $http.get('http://www.w3schools.com/website/customers_mysql.php');
                    /t }
                    }]);

                    app.controller('myController', ['$scope', 'MyService', function($scope, MyService) {
                    /t $scope.toUpperCaseFunction = function(data) {
                    /t/t $scope.myOutput = MyService.myFunction(data);
                    /t };
                    }]);
                 ]]>
            </scriptcode>
            <stylecode>
                <![CDATA[

                 ]]>
            </stylecode>
        </code>
        <example>
            <htmlcode>
                <![CDATA[
                    <div ng-app="myApp" ng-controller="myController">
                    /t <h4>Service Example</h4>
                    /t <div>Uppercase: <input ng-model="myInput" ng-change="toUpperCaseFunction(myInput)"></div><br />
                    /t <h2>{{myOutput}}</h2>

                    /t <h4>Exercise 1</h4>
                    /t <div>Lowercase: <input ng-model="lowercaseInput" ng-change="toLowerCaseFunction(lowercaseInput)"></div>
                    /t <h2>{{lowercaseOutput}}</h2>

                    /t <h4>Exercise 2</h4>
                    /t <h3 ng-repeat="details in list">{{details.Name}}</h3>
                    </div>
                ]]>
            </htmlcode>
            <scriptcode>
                <![CDATA[
                    var app = angular.module('myApp', []);

                    app.service('MyService', function() {
                    /t this.myFunction = function (data) {
                    /t/t return data.toUpperCase();
                    /t }
                    });

                    app.service('ToLowerCaseService', function() {
                    /t this.myFunction = function (data) {
                    /t/t return data.toLowerCase();
                    /t }
                    });

                    app.service('AnotherService', ['$http', function($http) {
                    /t this.getDetails = function () {
                    /t/t return $http.get('http://www.w3schools.com/website/customers_mysql.php');
                    /t }
                    }]);

                    app.controller('myController', ['$scope', 'MyService', 'ToLowerCaseService', 'AnotherService', function($scope, MyService, ToLowerCaseService, AnotherService) {
                    /t $scope.toUpperCaseFunction = function(data) {
                    /t/t $scope.myOutput = MyService.myFunction(data);
                    /t };
                    /t $scope.toLowerCaseFunction = function(data) {
                    /t/t $scope.lowercaseOutput = ToLowerCaseService.myFunction(data);
                    /t };
                    /t AnotherService.getDetails()
                    /t/t .then(function (response) {
                    /t/t/t $scope.list = response.data;
                    /t/t });
                    }]);
                 ]]>
            </scriptcode>
            <stylecode>
                <![CDATA[

                 ]]>
            </stylecode>
        </example>
    </chapter>
    <chapter>
        <level>
            Intermediate
        </level>
        <title>
            Dependency Injection
        </title>
        <content>
            Dependency Injection (DI) is a software design pattern that deals with how components get hold of their dependencies.

            The Angular injector subsystem is in charge of creating components, resolving their dependencies, and providing them to other components as requested.

            Dependency Annotation
            Angular invokes certain functions (like service factories and controllers) via the injector. You need to annotate these functions so that the injector knows what services to inject into the function. There are three ways of annotating your code with service name information:

            Using the inline array annotation (preferred)
            Using the $inject property annotation
            Implicitly from the function parameter names (has caveats)

            Example:

            Inline Array Annotation
            This is the preferred way to annotate application components. This is how the examples in the documentation are written.

            someModule.controller('MyController', ['$scope', 'greeter', function($scope, greeter) {
                // ...
            }]);

            Here we pass an array whose elements consist of a list of strings (the names of the dependencies) followed by the function itself.

            When using this type of annotation, take care to keep the annotation array in sync with the parameters in the function declaration.


            $inject Property Annotation
            To allow the minifiers to rename the function parameters and still be able to inject the right services, the function needs to be annotated with the $inject property. The $inject property is an array of service names to inject.

            var MyController = function($scope, greeter) {
                // ...
            }
            MyController.$inject = ['$scope', 'greeter'];
            someModule.controller('MyController', MyController);

            In this scenario the ordering of the values in the $inject array must match the ordering of the parameters in MyController.

            Just like with the array annotation, you'll need to take care to keep the $inject in sync with the parameters in the function declaration.

            Implicit Annotation
            The simplest way to get hold of the dependencies is to assume that the function parameter names are the names of the dependencies.

            someModule.controller('MyController', function($scope, greeter) {
                // ...
            });

            Given a function, the injector can infer the names of the services to inject by examining the function declaration and extracting the parameter names. In the above example, $scope and greeter are two services which need to be injected into the function.

            One advantage of this approach is that there's no array of names to keep in sync with the function parameters. You can also freely reorder dependencies.

            However this method will not work with JavaScript minifiers/obfuscators because of how they rename parameters.

            Tools like ng-annotate let you use implicit dependency annotations in your app and automatically add inline array annotations prior to minifying. If you decide to take this approach, you probably want to use ng-strict-di.

            Because of these caveats, we recommend avoiding this style of annotation.

            Exercise:

            Supply the needed dependency on the controller in order for the app to work.
        </content>
        <code>
            <htmlcode>
                <![CDATA[
                    <div ng-app="myApp" ng-controller="myController">
                    /t Input first Number: <input ng-model="a"><br />
                    /t Input second Number: <input ng-model="b"><br />
                    /t <button ng-click="compute(a, b)">Compute</button><br />
                    /t <h3>Sum is: {{sum}}</h3>
                    /t <h3>Difference is: {{difference}}</h3>
                    /t <h3>Product is: {{product}}</h3>
                    /t <h3>Quotient is: {{quotient}}</h3>
                    </div>
                ]]>
            </htmlcode>
            <scriptcode>
                <![CDATA[
                    var app = angular.module('myApp', []);

                    app.controller('myController', [function() {
                    /t $scope.compute = function(a, b) {
                    /t/t $scope.sum = ComputeService.getSum(a, b);
                    /t/t $scope.difference = ComputeService.getDifference(a, b);
                    /t/t $scope.product = ComputeService.getProduct(a, b);
                    /t/t $scope.quotient = ComputeService.getQuotient(a, b);
                    /t };
                    }]);

                    app.service('ComputeService', [function() {
                    /t this.getSum = function (a,b) {
                    /t/t return parseFloat(a) + parseFloat(b);
                    /t };
                    /t this.getDifference = function (a,b) {
                    /t/t return a - b;
                    /t };
                    /t this.getProduct = function (a,b) {
                    /t/t return a * b;
                    /t };
                    /t this.getQuotient = function (a,b) {
                    /t/t return a / b;
                    /t };
                    }]);
                 ]]>
            </scriptcode>
            <stylecode>
                <![CDATA[

                 ]]>
            </stylecode>
        </code>
        <example>
            <htmlcode>
                <![CDATA[
                    <div ng-app="myApp" ng-controller="myController">
                    /t Input first Number: <input ng-model="a"><br />
                    /t Input second Number: <input ng-model="b"><br />
                    /t <button ng-click="compute(a, b)">Compute</button><br />
                    /t <h3>Sum is: {{sum}}</h3>
                    /t <h3>Difference is: {{difference}}</h3>
                    /t <h3>Product is: {{product}}</h3>
                    /t <h3>Quotient is: {{quotient}}</h3>
                    </div>
                ]]>
            </htmlcode>
            <scriptcode>
                <![CDATA[
                    var app = angular.module('myApp', []);

                    app.controller('myController', ['$scope', 'ComputeService', function($scope, ComputeService) {
                    /t $scope.compute = function(a, b) {
                    /t/t $scope.sum = ComputeService.getSum(a, b);
                    /t/t $scope.difference = ComputeService.getDifference(a, b);
                    /t/t $scope.product = ComputeService.getProduct(a, b);
                    /t/t $scope.quotient = ComputeService.getQuotient(a, b);
                    /t };
                    }]);

                    app.service('ComputeService', [function() {
                    /t this.getSum = function (a,b) {
                    /t/t return parseFloat(a) + parseFloat(b);
                    /t };
                    /t this.getDifference = function (a,b) {
                    /t/t return a - b;
                    /t };
                    /t this.getProduct = function (a,b) {
                    /t/t return a * b;
                    /t };
                    /t this.getQuotient = function (a,b) {
                    /t/t return a / b;
                    /t };
                    }]);
                 ]]>
            </scriptcode>
            <stylecode>
                <![CDATA[

                 ]]>
            </stylecode>
        </example>
    </chapter>
    <chapter>
        <level>
            Intermediate
        </level>
        <title>
            Filters
        </title>
        <content>
            Filters format the value of an expression for display to the user. They can be used in view templates, controllers or services.
            Angular comes with a collection of built-in filters, but it is easy to define your own as well.

            Adding Filters to Expressions
            Filters can be added to expressions by using the pipe character |, followed by a filter.

            The uppercase filter format strings to upper case:

            &lt;div ng-app="myApp" ng-controller="personCtrl">

                &lt;p>The name is {{ lastName | uppercase }}&lt;/p>

            &lt;/div>

            Adding Filters to Directives
            Filters are added to directives, like ng-repeat, by using the pipe character |, followed by a filter:

            &lt;div ng-app="myApp" ng-controller="namesCtrl">

                &lt;ul>
                    &lt;li ng-repeat="x in names | orderBy:'country'">
                        {{ x.name + ', ' + x.country }}
                    &lt;/li>
                &lt;/ul>

            &lt;/div>

            You can create your own custom filter using the angular filter() function.

            Exercise:

            1. See our example in the Code Editor. Use Uppercase filter on the first Expression and Lowercase filter on the other Expression.

            2. On our second example you need to supply the correct Directive filter so that the names will be filtered based on what you type on the text input.
        </content>
        <code>
            <htmlcode>
                <![CDATA[
                    <div ng-app="myApp" ng-controller="myController">
                    /t <!-- Expression filters -->
                    /t <h3>Exercise 1</h3>
                    /t <p>Uppercase: {{ name }}</p>
                    /t <p>Lowercase: {{ name }}</p>

                    /t <!-- Directive filters -->
                    /t <h3>Exercise 2</h3>
                    /t <input type="text" ng-model="test">
                    /t <h2 ng-repeat="x in details">{{x.Name}}</h2>
                    </div>
                ]]>
            </htmlcode>
            <scriptcode>
                <![CDATA[
                    var app = angular.module('myApp', []);

                    app.controller('myController', ['$scope', 'AnotherService', function ($scope, AnotherService) {
                    /t $scope.name = 'John Doe';
                    /t AnotherService.getDetails().then(function (response) {
                    /t/t $scope.details = response.data;
                    /t });
                    }]);

                    app.service('AnotherService', ['$http', function($http) {
                    /t this.getDetails = function () {
                    /t/t return $http.get('http://www.w3schools.com/website/customers_mysql.php');
                    /t }
                    }]);
                 ]]>
            </scriptcode>
            <stylecode>
                <![CDATA[

                 ]]>
            </stylecode>
        </code>
        <example>
            <htmlcode>
                <![CDATA[
                    <div ng-app="myApp" ng-controller="myController">
                    /t <!-- Expression filters -->
                    /t <h3>Exercise 1</h3>
                    /t <p>Uppercase: {{ name | uppercase}}</p>
                    /t <p>Lowercase: {{ name | lowercase}}</p>

                    /t <!-- Directive filters -->
                    /t <h3>Exercise 2</h3>
                    /t <input type="text" ng-model="test">
                    /t <h2 ng-repeat="x in details | filter : test">{{x.Name}}</h2>
                    </div>
                ]]>
            </htmlcode>
            <scriptcode>
                <![CDATA[
                    var app = angular.module('myApp', []);

                    app.controller('myController', ['$scope', 'AnotherService', function ($scope, AnotherService) {
                    /t $scope.name = 'John Doe';
                    /t AnotherService.getDetails().then(function (response) {
                    /t/t $scope.details = response.data;
                    /t });
                    }]);

                    app.service('AnotherService', ['$http', function($http) {
                    /t this.getDetails = function () {
                    /t/t return $http.get('http://www.w3schools.com/website/customers_mysql.php');
                    /t }
                    }]);
                 ]]>
            </scriptcode>
            <stylecode>
                <![CDATA[

                 ]]>
            </stylecode>
        </example>
    </chapter>
    <chapter>
        <level>
            Intermediate
        </level>
        <title>
            Forms
        </title>
        <content>
            Controls (input, select, textarea) are ways for a user to enter data. A Form is a collection of controls for the purpose of grouping related controls together.

            Form and controls provide validation services, so that the user can be notified of invalid input before submitting a form. This provides a better user experience than server-side validation alone because the user gets instant feedback on how to correct the error.

            Server-side validation is still necessary for a secure application.

            Simple form

            The key directive in understanding two-way data-binding is ngModel. The ngModel directive provides the two-way data-binding by synchronizing the model to the view, as well as view to the model.
        
            Example:

            &lt;div ng-app="formExample" ng-controller="ExampleController">
            &lt;form novalidate class="simple-form">
                Name: &lt;input type="text" ng-model="user.name" />&lt;br />
                Gender: &lt;input type="radio" ng-model="user.gender" value="male" />male
                &lt;input type="radio" ng-model="user.gender" value="female" />female&lt;br />
                &lt;input type="button" ng-click="reset()" value="Reset" />
                &lt;input type="submit" ng-click="update(user)" value="Save" />
            &lt;/form>
            &lt;pre>user = {{user | json}}&lt;/pre>
            &lt;pre>master = {{master | json}}&lt;/pre>
            &lt;/div>

            &lt;script>
            angular.module('formExample', [])
                .controller('ExampleController', ['$scope', function($scope) {
                $scope.master = {};

                $scope.update = function(user) {
                    $scope.master = angular.copy(user);
                };

                $scope.reset = function() {
                    $scope.user = angular.copy($scope.master);
                };

                $scope.reset();
                }]);
            &lt;/script>

            Exercise:

            Try to recreate the example form and make it work. Also add two new functions namely Delete and Insert. The Delete Function will just delete the content of the user and master Scope. While the Insert function will automatically insert a name and gender value.
        </content>
        <code>
            <htmlcode>
                <![CDATA[
                    <div ng-app="formExample" ng-controller="ExampleController">
                    /t <form novalidate class="simple-form">
                    /t/t Name: <input type="text" ng-model="user.name" /><br />
                    /t/t Gender: <input type="radio" ng-model="user.gender" value="male" />male
                    /t/t/t <input type="radio" ng-model="user.gender" value="female" />female<br />
                    /t/t <input type="button" ng-click="reset()" value="Reset" />
                    /t/t <input type="submit" ng-click="update(user)" value="Save" />
                    /t </form>
                        
                    /t <pre>user = {{user | json}}</pre>
                    /t <pre>master = {{master | json}}</pre>
                    </div>
                ]]>
            </htmlcode>
            <scriptcode>
                <![CDATA[
                    angular.module('formExample', [])
                    /t .controller('ExampleController', ['$scope', function($scope) {
                    /t }]);
                 ]]>
            </scriptcode>
            <stylecode>
                <![CDATA[

                 ]]>
            </stylecode>
        </code>
        <example>
            <htmlcode>
                <![CDATA[
                    <div ng-app="formExample" ng-controller="ExampleController">
                    /t <form novalidate class="simple-form">
                    /t/t Name: <input type="text" ng-model="user.name" /><br />
                    /t/t Gender: <input type="radio" ng-model="user.gender" value="male" />male
                    /t/t/t <input type="radio" ng-model="user.gender" value="female" />female<br />
                    /t/t <input type="button" ng-click="reset()" value="Reset" />
                    /t/t <input type="button" ng-click="delete()" value="Delete" />
                    /t/t <input type="button" ng-click="insert()" value="Insert" />
                    /t/t <input type="submit" ng-click="update(user)" value="Save" />
                    /t </form>
                        
                    /t <pre>user = {{user | json}}</pre>
                    /t <pre>master = {{master | json}}</pre>
                    </div>
                ]]>
            </htmlcode>
            <scriptcode>
                <![CDATA[
                    angular.module('formExample', [])
                    /t .controller('ExampleController', ['$scope', function($scope) {
                    /t/t $scope.master = {};
                    /t/t $scope.update = function(user) {
                    /t/t/t $scope.master = angular.copy(user);
                    /t/t };
                    /t/t $scope.reset = function() {
                    /t/t/t $scope.user = angular.copy($scope.master);
                    /t/t };
                    /t/t $scope.delete = function() {
                    /t/t/t $scope.user = {};
                    /t/t/t $scope.master = {};
                    /t/t };
                    /t/t $scope.insert = function() {
                    /t/t/t $scope.user.name = 'Mark';
                    /t/t/t $scope.user.gender = 'male';
                    /t/t };
                    /t/t $scope.reset();
                    /t }]);
                 ]]>
            </scriptcode>
            <stylecode>
                <![CDATA[

                 ]]>
            </stylecode>
        </example>
    </chapter>
    <chapter>
        <level>
            Intermediate
        </level>
        <title>
            Routing
        </title>
        <content>
            If you want to navigate to different pages in your application, but you also want the application to be a SPA (Single Page Application), with no page reloading, you can use the ngRoute module.

            The ngRoute module routes your application to different pages without reloading the entire application.

            The ngRoute module helps your application to become a Single Page Application.
        
            Example:

            &lt;body ng-app="myApp">

            &lt;p>&lt;a href="#/">Main&lt;/a>&lt;/p>

            &lt;a href="#red">Red&lt;/a>
            &lt;a href="#green">Green&lt;/a>
            &lt;a href="#blue">Blue&lt;/a>

            &lt;div ng-view>&lt;/div>

            &lt;script>
                var app = angular.module("myApp", ["ngRoute"]);
                app.config(function($routeProvider) {
                    $routeProvider
                    .when("/", {
                        templateUrl : "main.htm"
                    })
                    .when("/red", {
                        templateUrl : "red.htm"
                    })
                    .when("/green", {
                        templateUrl : "green.htm"
                    })
                    .when("/blue", {
                        templateUrl : "blue.htm"
                    });
                });
            &lt;/script>

            To make your applications ready for routing, you must include the AngularJS Route module:

            &lt;script src="http://ajax.googleapis.com/ajax/libs/angularjs/1.4.8/angular-route.js">&lt;/script>

            Then you must add the ngRoute as a dependency in the application module::

            var app = angular.module("myApp", ["ngRoute"]);

            Now your application has access to the route module, which provides the $routeProvider.

            Use the $routeProvider to configure different routes in your application:

            app.config(function($routeProvider) {
                $routeProvider
                .when("/", {
                    templateUrl : "main.htm"
                })
                .when("/red", {
                    templateUrl : "red.htm"
                })
                .when("/green", {
                    templateUrl : "green.htm"
                })
                .when("/blue", {
                    templateUrl : "blue.htm"
                });
            });

            ngView

            Your application needs a container to put the content provided by the routing.

            This container is the ng-view directive.

            There are three different ways to include the ng-view directive in your application:

            Example:

            &lt;div ng-view>&lt;/div>
            &lt;ng-view>&lt;/ng-view>
            &lt;div class="ng-view">&lt;/div>

            $routeProvider
            With the $routeProvider you can define what page to display when a user clicks a link.

            var app = angular.module("myApp", ["ngRoute"]);
            app.config(function($routeProvider) {
                $routeProvider
                .when("/", {
                    templateUrl : "main.htm"
                })
                .when("/london", {
                    templateUrl : "london.htm"
                })
                .when("/paris", {
                    templateUrl : "paris.htm"
                });
            });

            Define the $routeProvider using the config method of your application. Work registered in the config method will be performed when the application is loading.

            Controllers
            With the $routeProvider you can also define a controller for each "view".

            var app = angular.module("myApp", ["ngRoute"]);
            app.config(function($routeProvider) {
                $routeProvider
                .when("/", {
                    templateUrl : "main.htm"
                })
                .when("/london", {
                    templateUrl : "london.htm",
                    controller : "londonCtrl"
                })
                .when("/paris", {
                    templateUrl : "paris.htm",
                    controller : "parisCtrl"
                });
            });
            app.controller("londonCtrl", function ($scope) {
                $scope.msg = "I love London";
            });
            app.controller("parisCtrl", function ($scope) {
                $scope.msg = "I love Paris";
            });

            Template

            In the previous examples we have used the templateUrl property in the $routeProvider.when method.

            You can also use the template property, which allows you to write HTML directly in the property value, and not refer to a page.

            var app = angular.module("myApp", ["ngRoute"]);
            app.config(function($routeProvider) {
                $routeProvider
                    .when("/", {
                        template : "&lt;h1>Main&lt;/h1>&lt;p>Click on the links to change this content&lt;/p>"
                    })
                    .when("/banana", {
                        template : "&lt;h1>Banana&lt;/h1>&lt;p>Bananas contain around 75% water.&lt;/p>"
                    })
                    .when("/tomato", {
                        template : "&lt;h1>Tomato&lt;/h1>&lt;p>Tomatoes contain around 95% water.&lt;/p>"
                    });
            });

            The otherwise method

            In the previous examples we have used the when method of the $routeProvider.

            You can also use the otherwise method, which is the default route when none of the others get a match.

            var app = angular.module("myApp", ["ngRoute"]);
            app.config(function($routeProvider) {
                $routeProvider
                    .when("/banana", {
                        template : "&lt;h1>Banana&lt;/h1>&lt;p>Bananas contain around 75% water.&lt;/p>"
                    })
                    .when("/tomato", {
                        template : "&lt;h1>Tomato&lt;/h1>&lt;p>Tomatoes contain around 95% water.&lt;/p>"
                    })
                    .otherwise({
                        template : "&lt;h1>None&lt;/h1>&lt;p>Nothing has been selected,&lt;/p>"
                    });
            });

            Exercise:

            Create your own Routing with at least three routes. You can just use Template instead of TemplateUrl. Also use the Otherwise method. Don't forget to add ngRoute to your module.
        </content>
        <code>
            <htmlcode>
                <![CDATA[
                    <div ng-app="myApp">
                    /t <p><a href="#/">Main</a></p> 
                    /t <a href="#route1">Route 1</a>
                    /t <a href="#route2">Route 2</a>
                    /t <a href="#route3">Route 3</a>
                        
                    /t <div ng-view></div>
                    </div> 
                ]]>
            </htmlcode>
            <scriptcode>
                <![CDATA[
                    var app = angular.module('myApp', []);
                 ]]>
            </scriptcode>
            <stylecode>
                <![CDATA[

                 ]]>
            </stylecode>
        </code>
        <example>
            <htmlcode>
                <![CDATA[
                    <div ng-app="myApp">
                    /t <p><a href="#/">Main</a></p> 
                    /t <a href="#route1">Route 1</a>
                    /t <a href="#route2">Route 2</a>
                    /t <a href="#route3">Route 3</a>
                        
                    /t <div ng-view></div>
                    </div> 
                ]]>
            </htmlcode>
            <scriptcode>
                <![CDATA[
                    var app = angular.module('myApp', ['ngRoute']);

                    app.config(function($routeProvider) {
                    /t $routeProvider
                    /t/t .when('/', {
                    /t/t/t template : '<h1>Main</h1><p>Select a route</p>'
                    /t/t }).when('/route1', {
                    /t/t/t template : '<h1>Route 1</h1><p>You are in route 1</p>'
                    /t/t }).when('/route2', {
                    /t/t/t template : '<h1>Route 2</h1><p>Now in route 2</p>'
                    /t/t }).when('/route3', {
                    /t/t/t template : '<h1>Route 3</h1><p>Currently in route 3</p>'
                    /t/t }).otherwise({
                    /t/t/t template : '<h1>None</h1><p>Nothing has been selected</p>'
                    /t/t });
                    });
                 ]]>
            </scriptcode>
            <stylecode>
                <![CDATA[

                 ]]>
            </stylecode>
        </example>
    </chapter>
    <chapter>
        <level>
            Advance
        </level>
        <title>
            Custom Directives
        </title>
        <content>
            In addition to all the built-in AngularJS directives, you can create your own directives.

            New directives are created by using the .directive function.

            To invoke the new directive, make an HTML element with the same tag name as the new directive.

            New directives are created by using the .directive function.
            The restrict option is typically set to:
            'A' - only matches attribute name
            'E' - only matches element name
            'C' - only matches class name
            'M' - only matches comment

            Example:

            &lt;script>
                app.directive('myCustomDirective', function () {
                    return {
                        restrict: 'E', // Restriction for Element
                        replace: true, // If true, Replace the custom element with the template 
                        scope: {
                            list: '='
                        },
                        template: '&lt;a href="#" ng-repeat="x in list">{{x}}&lt;/a>'
                    };
                });
            &lt;/script>

            &lt;my-custom-directive list="myList">&lt;/my-custom-directive>

            Exercise:

            Create your own directive where you can use the object in our controller.
        </content>
        <code>
            <htmlcode>
                <![CDATA[
                    <div ng-app="myApp" ng-controller="myController">

                    </div>
                ]]>
            </htmlcode>
            <scriptcode>
                <![CDATA[
                    var app = angular.module('myApp', []);

                    app.controller('myController', ['$scope', function($scope) {
                    /t $scope.data = [
                    /t/t {name:'John', age:25, gender:'boy'},
                    /t/t {name:'Jessie', age:30, gender:'girl'},
                    /t/t {name:'Johanna', age:28, gender:'girl'},
                    /t/t {name:'Joy', age:15, gender:'girl'},
                    /t/t {name:'Mary', age:28, gender:'girl'},
                    /t/t {name:'Peter', age:95, gender:'boy'},
                    /t/t {name:'Sebastian', age:50, gender:'boy'},
                    /t/t {name:'Erika', age:27, gender:'girl'},
                    /t/t {name:'Patrick', age:40, gender:'boy'},
                    /t/t {name:'Samantha', age:60, gender:'girl'}
                    /t ];
                    }]);
                 ]]>
            </scriptcode>
            <stylecode>
                <![CDATA[

                 ]]>
            </stylecode>
        </code>
        <example>
            <htmlcode>
                <![CDATA[
                    <div ng-app="myApp" ng-controller="myController">
                    /t <my-directive list="data"></my-directive>
                    </div>
                ]]>
            </htmlcode>
            <scriptcode>
                <![CDATA[
                    var app = angular.module('myApp', []);

                    app.controller('myController', ['$scope', function($scope) {
                    /t $scope.data = [
                    /t/t {name:'John', age:25, gender:'boy'},
                    /t/t {name:'Jessie', age:30, gender:'girl'},
                    /t/t {name:'Johanna', age:28, gender:'girl'},
                    /t/t {name:'Joy', age:15, gender:'girl'},
                    /t/t {name:'Mary', age:28, gender:'girl'},
                    /t/t {name:'Peter', age:95, gender:'boy'},
                    /t/t {name:'Sebastian', age:50, gender:'boy'},
                    /t/t {name:'Erika', age:27, gender:'girl'},
                    /t/t {name:'Patrick', age:40, gender:'boy'},
                    /t/t {name:'Samantha', age:60, gender:'girl'}
                    /t ];
                    }]);

                    app.directive('myDirective', [function () {
                    /t return {
                    /t/t restrict: 'E',
                    /t/t replace: true,
                    /t/t scope: {
                    /t/t/t list: '='
                    /t/t },
                    /t/t template: '<div ng-repeat="x in list"><a href="#" ng-click="showDetails = !showDetails">{{x.name}}</a> <span ng-show="showDetails">Age: {{x.age}} Gender: {{x.gender}}</span></div>'
                    /t };
                    }]);
                 ]]>
            </scriptcode>
            <stylecode>
                <![CDATA[

                 ]]>
            </stylecode>
        </example>
    </chapter>
</lesson>